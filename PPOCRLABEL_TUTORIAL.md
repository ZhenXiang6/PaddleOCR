# PPOCRLabel 標註工具完整教學

## 📋 目錄

1. [概述](#概述)
2. [安裝](#安裝)
3. [基本操作](#基本操作)
4. [Detection 標註技巧](#detection-標註技巧)
5. [快捷鍵](#快捷鍵)
6. [常見問題](#常見問題)
7. [輸出格式](#輸出格式)

---

## 概述

**PPOCRLabel** 是 PaddleOCR 官方提供的半自動圖形化標註工具，支持：
- ✅ **自動標註**：使用預訓練模型自動檢測文字區域
- ✅ **手動修正**：調整邊界框位置和文字內容
- ✅ **批量處理**：快速標註大量圖片
- ✅ **即時預覽**：實時查看標註效果
- ✅ **直接輸出**：生成 PaddleOCR 訓練格式

---

## 安裝

### macOS / Linux

```bash
# 安裝 PPOCRLabel
pip install PPOCRLabel

# 驗證安裝
PPOCRLabel --help
```

### Windows

```bash
pip install PPOCRLabel
```

**系統需求**：
- Python 3.6+
- PaddlePaddle / PaddleOCR（自動安裝）
- PyQt5（自動安裝）

---

## 基本操作

### 1. 啟動 PPOCRLabel

```bash
# 中文界面
PPOCRLabel --lang ch

# 英文界面
PPOCRLabel --lang en

# 指定預訓練模型路徑（可選）
PPOCRLabel --lang ch --det_model_dir /path/to/det_model
```

**啟動成功**：會打開圖形界面窗口

### 2. 載入圖片

**方法 1：打開目錄**
1. 點擊 **「打開目錄」** 按鈕
2. 選擇包含圖片的文件夾
3. 圖片列表會顯示在左側

**方法 2：打開文件**
1. 點擊 **「打開文件」** 按鈕
2. 選擇單個圖片文件

**支持格式**：
- .jpg / .jpeg
- .png
- .bmp
- .tiff

### 3. 自動標註（推薦）

**步驟**：
1. 點擊 **「自動標註」** 按鈕（或按 `Ctrl+A`）
2. 等待模型處理（會自動檢測所有圖片）
3. 檢測結果會自動標註在圖片上

**狀態說明**：
- ✓ 綠色勾號：已標註
- X 紅色叉號：未標註
- → 箭頭：當前圖片

### 4. 查看和修正標註

**查看標註**：
- 標註框會顯示在圖片上（彩色矩形）
- 右側顯示識別的文字內容
- 每個框有唯一編號

**修正邊界框**：
1. 點擊要修正的框（框會變成紅色高亮）
2. 拖動四個角的控制點調整大小
3. 拖動框的中心移動位置
4. 按 `Enter` 確認

**修正文字內容**：
1. 雙擊右側的文字標籤
2. 修改文字內容
3. 按 `Enter` 確認

### 5. 手動添加標註

**繪製矩形框**（推薦）：
1. 點擊 **「矩形標註」** 按鈕（或按 `W`）
2. 在圖片上拖動鼠標繪製矩形框
3. 釋放鼠標後，輸入文字內容
4. 按 `Enter` 確認

**繪製多邊形框**（複雜形狀）：
1. 點擊 **「多邊形標註」** 按鈕（或按 `Q`）
2. 在圖片上點擊四個角（順時針）
3. 右鍵完成繪製
4. 輸入文字內容
5. 按 `Enter` 確認

### 6. 刪除標註

**刪除單個標註**：
1. 點擊要刪除的框
2. 按 `Delete` 或 `Backspace` 鍵

**刪除所有標註**：
1. 點擊 **「清除全部」** 按鈕

### 7. 保存標註

**自動保存**：
- PPOCRLabel 會自動保存標註到 `Label.txt`
- 切換圖片時自動保存當前標註

**手動保存**：
- 點擊 **「保存」** 按鈕（或按 `Ctrl+S`）

### 8. 導航

**下一張圖片**：
- 點擊 **「下一張」** 按鈕
- 或按 `D` 鍵

**上一張圖片**：
- 點擊 **「上一張」** 按鈕
- 或按 `A` 鍵

**跳轉到特定圖片**：
- 在左側圖片列表中點擊圖片名稱

---

## Detection 標註技巧

### 針對作文稿紙的標註策略

#### 1. 合併碎片化的檢測框

**問題**：自動標註可能將一句話切成多個小框

**解決方案**：
1. **刪除碎片框**：
   - 選中碎片框 → 按 `Delete` 刪除
   - 重複刪除所有屬於同一句話的小框

2. **重新繪製大框**：
   - 按 `W` 鍵啟動矩形標註
   - 繪製包含整句話的大框
   - 輸入文字內容（可簡化）

**示例**：

❌ **錯誤**（碎片化）：
```
框1: "水"
框2: "能"
框3: "載"
框4: "舟"
```

✅ **正確**（合併）：
```
框1: "水能載舟亦能覆舟"
```

#### 2. 文字標註可以簡化

**Detection 訓練不需要完全準確的文字內容**，只需要：
- 邊界框位置準確
- 文字內容大致正確即可

**簡化方法**：
1. **使用自動識別的文字**：即使有錯字也沒關係
2. **使用佔位符**：輸入 "###" 或 "***" 代表此區域
3. **只輸入關鍵字**：例如 "水能載舟..." 省略後續

#### 3. 忽略打印文字

**目標**：只標註手寫文字

**操作**：
- 打印表頭（"答題卷"、"作答區"等）→ **不標註**
- 格子編號（11, 15, 22 等）→ **不標註**
- 手寫文字 → **標註**

**方法**：
- 如果自動標註包含打印文字，直接刪除那些框
- 不要手動添加打印文字的標註

#### 4. 標註範圍

**包含完整文字**：
- 框要完全包含所有文字
- 留一點邊距（5-10 像素）
- 不要太緊貼文字邊緣

**示例**：
```
✅ 正確：框稍大於文字，有邊距
❌ 錯誤：框太緊，切到部分文字
❌ 錯誤：框太大，包含空白太多
```

#### 5. 批量標註技巧

**快速流程**：
1. **自動標註**（`Ctrl+A`）→ 等待完成
2. **快速檢查**：
   - 按 `D` 鍵逐張查看
   - 只修正明顯錯誤（碎片化、漏檢）
   - 不追求完美（80-90% 準確度即可）
3. **重點標註**：
   - 對碎片化嚴重的圖片重點修正
   - 對質量好的圖片快速跳過

**目標**：
- 500 張圖片 → 約 4-6 小時（含自動標註）
- 100 張圖片 → 約 1-2 小時

---

## 快捷鍵

### 常用快捷鍵

| 快捷鍵 | 功能 | 說明 |
|--------|------|------|
| `W` | 矩形標註 | 繪製矩形框（推薦） |
| `Q` | 多邊形標註 | 繪製四邊形框 |
| `D` | 下一張圖片 | 切換到下一張 |
| `A` | 上一張圖片 | 切換到上一張 |
| `Delete` | 刪除標註 | 刪除當前選中的框 |
| `Ctrl+S` | 保存 | 手動保存標註 |
| `Ctrl+A` | 自動標註 | 批量自動檢測 |
| `Ctrl++` | 放大 | 放大圖片 |
| `Ctrl+-` | 縮小 | 縮小圖片 |
| `Ctrl+W` | 關閉 | 關閉當前窗口 |
| `Enter` | 確認 | 確認當前操作 |
| `Esc` | 取消 | 取消當前操作 |

### 進階快捷鍵

| 快捷鍵 | 功能 |
|--------|------|
| `E` | 編輯多邊形 |
| `X` | 識別當前框的文字 |
| `Ctrl+E` | 編輯標籤 |
| `Ctrl+Shift+D` | 刪除圖片 |

---

## 常見問題

### Q1: 自動標註沒有反應？

**可能原因**：
1. 模型下載中（首次使用需要下載）
2. 圖片格式不支持
3. 圖片太大（建議 < 5MB）

**解決方案**：
- 等待模型下載完成（查看控制台輸出）
- 轉換圖片格式為 .jpg
- 調整圖片大小（保持長邊 < 2000px）

### Q2: 標註框顯示不完整？

**原因**：圖片縮放問題

**解決方案**：
- 使用 `Ctrl++` 放大圖片
- 調整窗口大小
- 拖動圖片查看不同區域

### Q3: 如何修改已保存的標註？

**方法**：
1. 重新打開 PPOCRLabel
2. 打開標註過的圖片目錄
3. 會自動載入 `Label.txt` 中的標註
4. 修改後自動保存

### Q4: 標註文件保存在哪裡？

**位置**：
- `Label.txt` - 與圖片目錄同級
- `crop_img/` - 裁剪的文字圖片（如果啟用）
- `cache.cach` - 緩存文件

**示例**：
```
essay_images/
├── essay_001.jpg
├── essay_002.jpg
├── Label.txt          # 標註文件
├── crop_img/          # 裁剪圖片
│   ├── essay_001_0.jpg
│   ├── essay_001_1.jpg
│   └── ...
└── fileState.txt      # 文件狀態
```

### Q5: 可以暫停標註嗎？

**可以！**

PPOCRLabel 會自動保存進度：
- 關閉程序前會自動保存
- 下次打開同一目錄會恢復進度
- `fileState.txt` 記錄每張圖片的狀態

### Q6: 標註錯誤如何修正？

**方法**：
1. 點擊錯誤的框
2. 調整邊界或修改文字
3. 或直接刪除 (`Delete`) 重新標註

### Q7: 如何批量修正碎片化？

**快速方法**：
1. 啟用「合併模式」（如果有）
2. 或使用腳本後處理（合併相鄰的短文字框）
3. 建議：標註時直接重繪大框（更快）

---

## 輸出格式

### Label.txt 格式

PPOCRLabel 輸出的 `Label.txt` 格式：

```
image_name[TAB][JSON標註]
```

**範例**：
```txt
essay_001.jpg	[{"transcription": "水能載舟亦能覆舟", "points": [[100, 50], [500, 50], [500, 100], [100, 100]], "difficult": false}, {"transcription": "活中擁有健康的人際關係", "points": [[100, 120], [480, 120], [480, 170], [100, 170]], "difficult": false}]
```

**字段說明**：
- `transcription`: 文字內容
- `points`: 四個角的座標 `[[x1,y1], [x2,y2], [x3,y3], [x4,y4]]`
- `difficult`: 是否為困難樣本（一般為 `false`）

### 直接用於 PaddleOCR 訓練

**Detection 訓練**：
- `Label.txt` 可以直接作為 Detection 訓練的標註文件
- 使用 `prepare_det_data.py` 腳本處理（分割訓練/驗證集）

**Recognition 訓練**：
- PPOCRLabel 會自動裁剪文字區域到 `crop_img/`
- 生成 `rec_gt.txt` 標註文件
- 格式：`crop_img/essay_001_0.jpg[TAB]水能載舟亦能覆舟`

---

## 進階功能

### 1. 啟用識別結果保存

**設置**：
1. 點擊「文件」→「保存識別結果」
2. 勾選「保存識別結果」

**效果**：
- 自動裁剪每個文字區域
- 保存到 `crop_img/` 目錄
- 生成 `rec_gt.txt` 用於 Recognition 訓練

### 2. 使用自定義模型

**啟動時指定**：
```bash
PPOCRLabel --lang ch \
  --det_model_dir /path/to/custom_det_model \
  --rec_model_dir /path/to/custom_rec_model
```

**用途**：
- 使用 Fine-tuned 模型進行自動標註
- 提高自動標註準確度

### 3. 批量重新識別

**場景**：標註了邊界框，但文字內容需要重新識別

**操作**：
1. 選中要重新識別的框
2. 按 `X` 鍵
3. 或點擊「重新識別」按鈕

---

## 標註質量檢查

### 檢查清單

完成標註後，檢查以下項目：

- [ ] 所有手寫文字都已標註
- [ ] 打印文字已忽略（不標註）
- [ ] 碎片化的框已合併
- [ ] 邊界框完整包含文字
- [ ] 文字內容大致正確（或使用佔位符）
- [ ] 沒有重複標註
- [ ] `Label.txt` 文件已生成

### 質量指標

**合格標準**：
- 標註覆蓋率 > 95%（漏標 < 5%）
- 邊界框準確度 > 90%
- 碎片化比例 < 10%

**不需要 100% 完美**：
- Detection 訓練對文字內容容忍度高
- 邊界框位置比文字內容更重要
- 80-90% 準確度即可取得良好效果

---

## 快速開始

### 5 分鐘快速體驗

1. **安裝**：
   ```bash
   pip install PPOCRLabel
   ```

2. **啟動**：
   ```bash
   PPOCRLabel --lang ch
   ```

3. **載入圖片**：
   - 點擊「打開目錄」
   - 選擇包含作文圖片的文件夾

4. **自動標註**：
   - 點擊「自動標註」（或 `Ctrl+A`）
   - 等待完成

5. **快速修正**：
   - 按 `D` 鍵逐張檢查
   - 刪除碎片框 (`Delete`)
   - 重繪大框 (`W`)

6. **完成**：
   - 標註會自動保存到 `Label.txt`
   - 關閉程序即可

### 推薦工作流程

**Day 1：批量自動標註**（1-2 小時）
1. 準備所有圖片到一個目錄
2. 啟動 PPOCRLabel
3. 執行自動標註
4. 快速瀏覽，標記需要重點修正的圖片

**Day 2：重點修正**（2-4 小時）
1. 只修正碎片化嚴重的圖片
2. 合併短文字框
3. 補充漏檢的文字
4. 完成標註

**總時間**：約 3-6 小時（500 張圖片）

---

## 參考資源

- [PPOCRLabel GitHub](https://github.com/PaddlePaddle/PaddleOCR/tree/main/PPOCRLabel)
- [PPOCRLabel 視頻教學](https://www.bilibili.com/video/BV1XK411L7Gw)
- [PaddleOCR 官方文檔](https://paddlepaddle.github.io/PaddleOCR/)

---

## 下一步

完成標註後：

1. ✅ 使用 `prepare_det_data.py` 處理標註文件
2. ✅ 分割訓練集和驗證集
3. ✅ 開始 Detection 模型訓練
4. ✅ 評估模型效果

**祝標註順利！** 🎯
